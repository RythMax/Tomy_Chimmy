@model IEnumerable<PayingMethod>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FormaDePago)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FormaDePago)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Method_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Method_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Method_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
